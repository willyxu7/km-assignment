<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.km.assignment.repository.UserRepository">
    <resultMap id="result" type="user">
        <id property="id" column="ID"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="deleted" column="deleted"/>
        <result property="creator" column="creator"/>
        <result property="createdTime" column="created_time"/>
    </resultMap>
    
    <select id="getOne" resultType="user" resultMap="result">
        SELECT * FROM user LIMIT 1
    </select>

    <select id="findById" parameterType="String" resultType="user" resultMap="result">
        SELECT * FROM user WHERE ID = #{id}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) FROM user
    </select>
    
    <insert id="insert" parameterType="user" keyProperty="id">
        INSERT INTO user(ID, code, name, status, remark, deleted, creator, created_time)
        VALUES (#{id}, #{code}, #{name}, #{status}, #{remark}, #{deleted}, #{creator}, #{createdTime})
    </insert>
</mapper>